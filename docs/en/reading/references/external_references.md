# External References Log

> This is a reference log, not a canonical contract. The active truth lives in `docs/en/explanation`, `docs/en/reference`, and `docs/en/how-to`.

## Usage rule

External papers and articles are evidence, inspiration, or background. They do not override the repo’s own active contract.

## External references tied directly to the current implementation

- Catmull-Rom curve references for guide resampling
- Gaussian / normalized anchor blending references for guide-local support and coordinates
- skeleton-based seamless blending references as conceptual support for field-like structure

## Background references

- implicit surface history
- field-based interpretation of spatial scoring
- documentation-style references for docs quality

## Relation to the current code

These materials help explain why the current implementation looks the way it does, but the exact active formulas still live in [Current Formula Reference](../../reference/current_formula_reference.md).

## Reference links

The detailed source list remains in the Korean counterpart and can be expanded later as needed.
